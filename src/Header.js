import React from "react";
import './Header.scss';

export default function Header() {
    return (
        <header>
            <h1>
                GULAABI ART
            </h1>
        </header>
    );
}